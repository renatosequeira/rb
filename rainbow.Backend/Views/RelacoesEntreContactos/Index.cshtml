@model IEnumerable<rainbow.Domain.Configurations.RelacaoEntreContactos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescricaoRelacao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescricaoRelacao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RelacaoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RelacaoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RelacaoId })
        </td>
    </tr>
}

</table>
