@model IEnumerable<rainbow.Domain.Animais.AnimalEstimacao>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cliente.NomeCliente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoAnimal.TipoAnimalDesignacao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NomeAnimalEstimacao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.NomeCliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoAnimal.TipoAnimalDesignacao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NomeAnimalEstimacao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AnimalEstimacaoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AnimalEstimacaoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AnimalEstimacaoId })
        </td>
    </tr>
}

</table>
