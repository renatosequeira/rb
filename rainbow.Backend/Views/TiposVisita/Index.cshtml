@model IEnumerable<rainbow.Domain.Configurations.TipoVisita>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeTipoVisita)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeTipoVisita)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoVisitaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoVisitaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoVisitaId })
        </td>
    </tr>
}

</table>
