@model IEnumerable<rainbow.Domain.Saude.Alergia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescricaoAlergia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescricaoAlergia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AlergiaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AlergiaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AlergiaId })
        </td>
    </tr>
}

</table>
