@model IEnumerable<rainbow.Domain.Configurations.TipoDeMaterial>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescricaoTipoDeMaterial)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescricaoTipoDeMaterial)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoDeMaterialId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoDeMaterialId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoDeMaterialId })
        </td>
    </tr>
}

</table>
